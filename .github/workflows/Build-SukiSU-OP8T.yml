name: Build SukiSU Kernel for OnePlus 8T (sm8250)

on:
  workflow_dispatch:
    inputs:
      KERNEL_BRANCH:
        description: "选择 LineageOS 内核分支（例如 lineage-23.0 或 lineage-22.1）"
        required: true
        default: "lineage-23.0"
      SUSFS_MODE:
        description: "选择 SUSFS 集成模式"
        required: true
        type: choice
        options:
          - none
          - main
          - dev
        default: none
      KPM:
        description: "启用 KPM 模块"
        type: boolean
        default: false

  schedule:
    - cron: "0 3 * * *"

jobs:
  build:
    name: Build SukiSU Kernel (${{ github.event.inputs.KERNEL_BRANCH || 'lineage-23.0' }})
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Workflow Repo
        uses: actions/checkout@v4

      - name: Setup Build Environment
        run: |
          sudo apt-get update -y
          sudo apt-get install -y git clang llvm lld make gcc g++ bc bison flex libssl-dev \
            libelf-dev libncurses5-dev libncursesw5-dev unzip python3 python3-pip wget curl zip \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

          mkdir -p $HOME/toolchains
          cd $HOME/toolchains
          wget https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/master/clang-r530567.tar.gz -O clang.tar.gz
          mkdir clang && tar -xf clang.tar.gz -C clang
          echo "export PATH=$HOME/toolchains/clang/bin:\$PATH" >> $GITHUB_ENV

      - name: Clone Kernel Source
        run: |
          BRANCH="${{ github.event.inputs.KERNEL_BRANCH || 'lineage-23.0' }}"
          git clone --depth=1 --branch "$BRANCH" \
            https://github.com/LineageOS/android_kernel_oneplus_sm8250 kernel_workspace
          cd kernel_workspace
          git submodule update --init --recursive

      - name: Integrate SukiSU (手动 Hook 模式)
        run: |
          cd kernel_workspace
          MODE="${{ github.event.inputs.SUSFS_MODE || 'none' }}"
          if [[ "$MODE" == "main" ]]; then
            echo ">>> 集成 SukiSU SUSFS-main 分支 (非 GKI 手动模式)"
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
          elif [[ "$MODE" == "dev" ]]; then
            echo ">>> 集成 SukiSU SUSFS-dev 分支 (开发版)"
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-dev
          else
            echo ">>> 跳过 SUSFS 集成"
          fi

      - name: Configure Kernel
        run: |
          cd kernel_workspace
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH=$HOME/toolchains/clang/bin:$PATH
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          make O=out vendor/bengal_defconfig

      - name: Build Kernel
        run: |
          cd kernel_workspace
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH=$HOME/toolchains/clang/bin:$PATH
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          # 输出详细编译日志
          make O=out -j$(nproc) V=1 2>&1 | tee build.log
        continue-on-error: true

      - name: Upload Build Log (if failed)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-build-log
          path: kernel_workspace/build.log

      - name: Create AnyKernel3 Flashable Zip
        if: success()
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
          cd AnyKernel3
          rm -rf *.img *.zip
          cp -f ../kernel_workspace/out/arch/arm64/boot/Image ./Image
          cp -f ../kernel_workspace/out/arch/arm64/boot/dtbo.img ./dtbo.img || true
          cp -f ../kernel_workspace/out/arch/arm64/boot/dtb ./dtb || true
          sed -i 's/is_slot_device=1/is_slot_device=0/g' anykernel.sh

          ZIP_NAME="SukiSU-OP8T-${{ github.event.inputs.KERNEL_BRANCH || 'lineage-23.0' }}-${{ github.event.inputs.SUSFS_MODE || 'none' }}.zip"
          zip -r9 "$ZIP_NAME" * -x .git README.md .gitignore
          mv "$ZIP_NAME" ../

      - name: Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: "SukiSU-OP8T-${{ github.event.inputs.KERNEL_BRANCH || 'lineage-23.0' }}-${{ github.event.inputs.SUSFS_MODE || 'none' }}"
          path: "SukiSU-OP8T-${{ github.event.inputs.KERNEL_BRANCH || 'lineage-23.0' }}-${{ github.event.inputs.SUSFS_MODE || 'none' }}.zip"
